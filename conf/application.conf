play.application.loader = AppLoader
play.http.secret.key = "changeme"
play.http.secret.key = ${?CRYPTO_SECRET}
play.i18n.langs = [ "en" ]

# apiKey is going to be overwritten by an $API_KEY env variable, if present */
apiKey = ""
apiKey = ${?API_KEY}
